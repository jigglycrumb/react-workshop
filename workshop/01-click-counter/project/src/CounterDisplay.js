/*
  This component represents the display of the current counter value
  It should render this HTML

  <h3>You clicked the button 0 times.</h3>

  It receives one prop:
    - counter: The current counter value
*/

export function CounterDisplay({ counter }) {
  return <div>this will display of the counter</div>;
}
